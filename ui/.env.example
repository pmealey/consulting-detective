# Copy this to .env.development for local dev.
# This file is only loaded by `npm run dev` (Vite development mode).
# Production builds use no .env file -- the /api fallback in client.ts
# routes through CloudFront automatically.
VITE_API_BASE_URL=https://YOUR_API_GATEWAY_ID.execute-api.us-east-1.amazonaws.com/prod
