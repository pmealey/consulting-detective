{
  "name": "consulting-detective",
  "version": "0.1.0",
  "bin": {
    "consulting-detective": "dist/bin/consulting-detective.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "dev": "npm run dev --prefix ui",
    "synth": "npx cdk synth",
    "diff": "npx cdk diff",
    "deploy": "npm run build && npm run build --prefix ui && npx cdk deploy",
    "destroy": "npx cdk destroy"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/node": "^22.0.0",
    "aws-cdk": "^2.175.0",
    "esbuild": "^0.24.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.7.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "aws-cdk-lib": "^2.175.0",
    "constructs": "^10.4.0"
  }
}
